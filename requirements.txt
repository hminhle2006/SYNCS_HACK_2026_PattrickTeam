# Lane B (geo) requirements — created because Lane A's scaffold wasn't pushed yet.
# Reconcile with Lane A before merging.
numpy>=1.26
pandas>=2.2
pyarrow>=16.0
shapely>=2.0
geopandas>=1.0
osmnx>=2.0
pvlib>=0.11
requests>=2.32
rtree>=1.3
pytest>=8.0
matplotlib>=3.9
contextily>=1.6
